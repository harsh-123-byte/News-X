<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsX" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/purple</item>
         <item name="colorPrimaryVariant">@color/purple</item>
         <item name="android:statusBarColor">@color/purple</item>
    </style>

    <style name="Theme.NewsX" parent="Base.Theme.NewsX" />
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView">
        <item name="android:color">@color/yellow</item>
    </style>
</resources>